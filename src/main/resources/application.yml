server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://47.106.240.56:3306/assistant?useUnicode=true&characterEncoding=utf-8
    username: root
    password: my-secret-pw

mybatis:
  mapper-locations:
    - classpath:mapper/*.xml
    - classpath*:com/**/mapper/*.xml
  type-aliases-package: com.assistant.windfall.dao

mapper:
  not-empty: false
  identity: MYSQL

pagehelper:
  helperDialect: mysql
  reasonable: true
  supportMethodsArguments: true
  params: count=countSql

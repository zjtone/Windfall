<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.assistant.windfall.mbg.mapper.OrgMapper">
    <resultMap id="OrgDao" type="com.assistant.windfall.dao.OrgDao">
        <id column="id" property="id" jdbcType="INTEGER"/>
        <result column="name" property="name" jdbcType="VARCHAR"/>
        <result column="description" property="description" jdbcType="VARCHAR"/>
        <result column="account" property="account" jdbcType="VARCHAR"/>
        <result column="password" property="password" jdbcType="VARCHAR"/>
    </resultMap>

    <select id="obtainOrgById" resultMap="OrgDao">
        SELECT id, name, description, email, password, status
        FROM Organization
        WHERE id = #{id}
    </select>

    <select id="listOrg" resultMap="OrgDao">
        SELECT id, name, description, email, password, status
        FROM Organization
        where orgId = #{orgId}
    </select>

    <insert id="insertOrg"
            parameterType="com.assistant.windfall.dao.OrgDao"
            flushCache="true"
            statementType="PREPARED"
            keyProperty="id"
            useGeneratedKeys="true"
            timeout="20">
        INSERT INTO Organization(name, description, email, password, status)
        VALUES(#{name}, #{description}, #{email}, #{password}, #{status})
    </insert>
</mapper>
